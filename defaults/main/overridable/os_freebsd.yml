# path to system sudoers.d directory
cfg_sudo_freebsd_sudoers_d_dir: "/usr/local/etc/sudoers.d"

# file group for sudoers config file (i.e. the primary group for user 'root')
cfg_sudo_freebsd_sudoers_file_group: 'wheel'

